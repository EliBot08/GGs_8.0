<UserControl x:Class="GGs.ErrorLogViewer.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             Background="{DynamicResource SystemControlBackgroundAltHighBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Settings" 
                           FontSize="24" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Configure log viewer preferences and behavior" 
                           FontSize="14" 
                           Opacity="0.7"/>
            </StackPanel>

            <!-- Appearance Settings -->
            <Border Grid.Row="1" 
                    Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Appearance" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,16"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" 
                                   Text="Theme" 
                                   VerticalAlignment="Center"
                                   Margin="0,0,0,12"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" 
                                    Orientation="Horizontal"
                                    Margin="0,0,0,12">
                            <Button Content="Toggle Theme" 
                                    Command="{Binding ToggleThemeCommand}"
                                    Padding="16,8"
                                    MinWidth="120"/>
                            <TextBlock Text="{Binding ThemeService.IsDarkMode, StringFormat='Current: {0}'}" 
                                       VerticalAlignment="Center"
                                       Margin="16,0,0,0"
                                       Opacity="0.7"/>
                        </StackPanel>

                        <TextBlock Grid.Row="1" Grid.Column="0" 
                                   Text="Font Size" 
                                   VerticalAlignment="Center"
                                   Margin="0,0,0,12"/>
                        <StackPanel Grid.Row="1" Grid.Column="1" 
                                    Orientation="Horizontal"
                                    Margin="0,0,0,12">
                            <Slider Width="200" 
                                    Minimum="10" 
                                    Maximum="20" 
                                    Value="{Binding LogFontSize}"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding LogFontSize, StringFormat='{}{0:F0}px'}" 
                                       VerticalAlignment="Center"
                                       Margin="12,0,0,0"
                                       MinWidth="40"/>
                        </StackPanel>

                        <TextBlock Grid.Row="2" Grid.Column="0" 
                                   Text="View Mode" 
                                   VerticalAlignment="Center"/>
                        <ToggleButton Grid.Row="2" Grid.Column="1" 
                                      Content="{Binding IsRawMode, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='Raw Mode|Compact Mode'}"
                                      IsChecked="{Binding IsRawMode}"
                                      HorizontalAlignment="Left"
                                      Padding="16,8"
                                      MinWidth="120"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Monitoring Settings -->
            <Border Grid.Row="2" 
                    Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Monitoring" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,16"/>
                    
                    <CheckBox Content="Auto-scroll to new entries" 
                              IsChecked="{Binding AutoScroll}"
                              Margin="0,0,0,12"/>
                    <CheckBox Content="Enable smart filtering" 
                              IsChecked="{Binding UseSmartFilter}"
                              Margin="0,0,0,12"/>
                    <CheckBox Content="Show notifications for errors" 
                              Margin="0,0,0,12"/>
                    <CheckBox Content="Automatically export daily summaries" 
                              Margin="0,0,0,12"/>
                    
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" 
                                   Text="Log Directory" 
                                   VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBox Text="{Binding LogDirectory}" 
                                     IsReadOnly="True"
                                     MinWidth="300"
                                     Margin="0,0,8,0"/>
                            <Button Content="Browse..." 
                                    Command="{Binding OpenLogDirectoryCommand}"
                                    Padding="16,8"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Performance Settings -->
            <Border Grid.Row="3" 
                    Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="Performance" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,16"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" 
                                   Text="Max entries in view" 
                                   VerticalAlignment="Center"
                                   Margin="0,0,0,12"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" 
                                    Orientation="Horizontal"
                                    Margin="0,0,0,12">
                            <Slider Width="200" 
                                    Minimum="1000" 
                                    Maximum="100000" 
                                    Value="10000"
                                    TickFrequency="5000"
                                    IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Text="10,000" 
                                       VerticalAlignment="Center"
                                       Margin="12,0,0,0"
                                       MinWidth="60"/>
                        </StackPanel>

                        <TextBlock Grid.Row="1" Grid.Column="0" 
                                   Text="Refresh interval (ms)" 
                                   VerticalAlignment="Center"/>
                        <StackPanel Grid.Row="1" Grid.Column="1" 
                                    Orientation="Horizontal">
                            <Slider Width="200" 
                                    Minimum="100" 
                                    Maximum="5000" 
                                    Value="500"
                                    TickFrequency="100"
                                    IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Text="500" 
                                       VerticalAlignment="Center"
                                       Margin="12,0,0,0"
                                       MinWidth="60"/>
                        </StackPanel>
                    </Grid>

                    <Button Content="Clear Cache and Reset" 
                            HorizontalAlignment="Left"
                            Margin="0,20,0,0"
                            Padding="20,10"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
