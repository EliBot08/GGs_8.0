<UserControl x:Class="GGs.Desktop.Views.Admin.DeviceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBox Width="240" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Search" Command="{Binding SearchCommand}"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Text="Page size:"/>
            <TextBox Width="50" Text="{Binding PageSize, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Prev" Command="{Binding PrevPageCommand}"/>
            <Button Content="Next" Command="{Binding NextPageCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Devices}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Device ID" Binding="{Binding DeviceId}" Width="*"/>
                <DataGridTextColumn Header="Common Name" Binding="{Binding CommonName}" Width="200"/>
                <DataGridTextColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
                <DataGridTextColumn Header="Last Seen" Binding="{Binding LastSeenUtc}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="2">
            <TextBlock Text="{Binding Status}"/>
        </StatusBar>
    </Grid>
</UserControl>

