<UserControl x:Class="GGs.Desktop.Views.Admin.RemoteManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Refresh Devices" Command="{Binding RefreshDevicesCommand}"/>
            <Button Content="Refresh Tweaks" Margin="6,0,0,0" Command="{Binding RefreshTweaksCommand}"/>
            <Button Content="Send Tweak" Margin="6,0,0,0" Command="{Binding SendTweakCommand}" IsEnabled="{Binding AllowedExecuteRemote}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="ToolTip" Value=""/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding AllowedExecuteRemote}" Value="False">
                                <Setter Property="ToolTip" Value="High-risk remote tweaks require Deep Optimization enabled and the Agent service running."/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,8,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Connected Devices" Margin="0,0,8,0">
                <ListBox ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}"/>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Tweaks">
                <DataGrid ItemsSource="{Binding Tweaks}" SelectedItem="{Binding SelectedTweak}" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding CommandType}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>

        <StatusBar Grid.Row="2">
            <TextBlock Text="{Binding Status}"/>
        </StatusBar>
    </Grid>
</UserControl>

