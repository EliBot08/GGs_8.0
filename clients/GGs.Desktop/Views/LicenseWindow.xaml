<Window x:Class="GGs.Desktop.Views.LicenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GGs - Activate License" Height="400" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Paste your license JSON below" FontSize="16" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Name="LicenseText" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Name="StatusText" VerticalAlignment="Center" Foreground="#555"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="ValidateBtn" Content="Validate" Width="120" Margin="0,0,10,0" Click="ValidateBtn_Click"/>
                <Button Name="ExitBtn" Content="Exit" Width="100" Click="ExitBtn_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
