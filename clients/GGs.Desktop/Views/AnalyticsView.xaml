<UserControl x:Class="GGs.Desktop.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:GGs.Desktop.Views.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibility" />
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
            <Button Content="Export Report" Margin="6,0,0,0" Command="{Binding ExportReportCommand}"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#FFF8D7" BorderBrush="#E0C200" BorderThickness="1" Padding="8" Margin="0,6,0,6" Visibility="{Binding BannerMessage, Converter={StaticResource NullOrEmptyToVisibility}}">
            <TextBlock Text="{Binding BannerMessage}"/>
        </Border>

        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Analytics Overview" FontWeight="Bold" FontSize="16"/>
                <UniformGrid Columns="2" Rows="2" Margin="0,8,0,8">
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="8" Margin="4">
                        <StackPanel>
                            <TextBlock Text="Total Tweaks Applied"/>
                            <TextBlock Text="{Binding TotalTweaksApplied}" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="8" Margin="4">
                        <StackPanel>
                            <TextBlock Text="Success Rate"/>
                            <TextBlock Text="{Binding SuccessRate, StringFormat={}{0:F2}%}" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="8" Margin="4">
                        <StackPanel>
                            <TextBlock Text="Most Popular Tweak"/>
                            <TextBlock Text="{Binding MostPopularTweak}" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="8" Margin="4">
                        <StackPanel>
                            <TextBlock Text="Active Licenses"/>
                            <TextBlock Text="{Binding ActiveLicenses}" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <TextBlock Text="Top Tweaks" FontWeight="Bold"/>
                <DataGrid ItemsSource="{Binding TweakStatistics}" AutoGenerateColumns="False" Height="220">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Applied" Binding="{Binding ApplicationCount}" Width="120"/>
                        <DataGridTextColumn Header="Success %" Binding="{Binding SuccessRate, StringFormat={}{0:F1}}" Width="120"/>
                        <DataGridTextColumn Header="Last Applied" Binding="{Binding LastApplied}" Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock Text="Recent Logs" FontWeight="Bold"/>
                <DataGrid ItemsSource="{Binding RecentLogs}" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tweak" Binding="{Binding TweakName}" Width="*"/>
                        <DataGridTextColumn Header="Device" Binding="{Binding DeviceId}" Width="160"/>
                        <DataGridTextColumn Header="Time" Binding="{Binding AppliedUtc}" Width="160"/>
                        <DataGridCheckBoxColumn Header="Success" Binding="{Binding Success}" Width="80"/>
                        <DataGridTextColumn Header="Before" Binding="{Binding BeforeState}" Width="220"/>
                        <DataGridTextColumn Header="After" Binding="{Binding AfterState}" Width="220"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>

