<UserControl x:Class="GGs.Desktop.Views.OnboardingChecklist"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border BorderBrush="#D0D4E4" BorderThickness="1" CornerRadius="6" Padding="10" Background="#F8FAFF">
        <StackPanel>
            <TextBlock Text="First-run checklist" FontWeight="Bold" FontSize="16"/>

            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <CheckBox x:Name="ServerCheck" Content="Connect to server" IsEnabled="False"/>
                <Button Content="Check" Margin="8,0,0,0" Click="CheckServer_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <CheckBox x:Name="LicenseCheck" Content="Activate license" IsEnabled="False"/>
                <Button Content="Open activation" Margin="8,0,0,0" Click="OpenActivation_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <CheckBox x:Name="BaselineCheck" Content="Run safe baseline" IsEnabled="False"/>
                <Button Content="Run baseline" Margin="8,0,0,0" Click="RunBaseline_Click"/>
            </StackPanel>

            <TextBlock x:Name="StatusText" Margin="0,8,0,0" Foreground="#666"/>
        </StackPanel>
    </Border>
</UserControl>

