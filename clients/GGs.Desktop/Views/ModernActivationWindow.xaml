<Window x:Class="GGs.Desktop.Views.ModernActivationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GGs - Login"
        Height="550" Width="450"
        MinHeight="550" MinWidth="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- Modern Purple Theme Colors -->
        <Color x:Key="BackgroundDark">#FF1A1625</Color>
        <Color x:Key="BackgroundMedium">#FF2A2438</Color>
        <Color x:Key="AccentPurple">#FF8B5CF6</Color>
        <Color x:Key="AccentPurpleLight">#FFA78BFA</Color>
        <Color x:Key="TextPrimary">#FFFFFFFF</Color>
        <Color x:Key="TextSecondary">#FF9CA3AF</Color>
        <Color x:Key="BorderColor">#FF3D3650</Color>
        
        <!-- Purple Gradient Brushes -->
        <LinearGradientBrush x:Key="PurpleGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF8B5CF6" Offset="0"/>
            <GradientStop Color="#FFA78BFA" Offset="1"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF1A1625" Offset="0"/>
            <GradientStop Color="#FF2D2438" Offset="1"/>
        </LinearGradientBrush>
        
        <!-- Animations -->
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <Storyboard x:Key="SlideUp">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" 
                           From="30" To="0" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <!-- Purple Login Button Style -->
        <Style x:Key="PurpleLoginButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PurpleGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="8" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="scale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scale" 
                                                           Storyboard.TargetProperty="ScaleX"
                                                           To="1.02" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="scale" 
                                                           Storyboard.TargetProperty="ScaleY"
                                                           To="1.02" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scale" 
                                                           Storyboard.TargetProperty="ScaleX"
                                                           To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="scale" 
                                                           Storyboard.TargetProperty="ScaleY"
                                                           To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PurpleGradient}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="BorderBrush" Value="{StaticResource PurpleGradient}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#FF8B5CF6" Opacity="0.1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Small Icon Button Style -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#FF2A2438"/>
            <Setter Property="Foreground" Value="#FFA78BFA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF3D3650"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- License Key TextBox Style -->
        <Style x:Key="LicenseKeyBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FF1A1F2E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF2A3041"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="CharacterCasing" Value="Upper"/>
            <Setter Property="MaxLength" Value="19"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FFFF1464"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- Main Window Border with Purple Theme -->
    <Border CornerRadius="20" BorderThickness="2">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF8B5CF6" Offset="0"/>
                <GradientStop Color="#FF3D3650" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF1A1625" Offset="0"/>
                <GradientStop Color="#FF2D2438" Offset="0.5"/>
                <GradientStop Color="#FF1A1625" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        
        <Grid>
            <!-- Main Content - Centered Login Card -->
            <Grid Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Close Button -->
                <Button Grid.Row="0" Content="✕" HorizontalAlignment="Right" 
                      Width="30" Height="30" Background="Transparent" 
                      Foreground="#FF9CA3AF" BorderThickness="0"
                      FontSize="16" Click="CloseButton_Click" Cursor="Hand"
                      Margin="0,-20,-20,0"/>
                
                <!-- Logo -->
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,20,0,10">
                    <TextBlock Text="GGs" FontSize="64" FontWeight="Bold" 
                             HorizontalAlignment="Center">
                        <TextBlock.Foreground>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#FF8B5CF6" Offset="0"/>
                                <GradientStop Color="#FFA78BFA" Offset="1"/>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>
                    </TextBlock>
                </StackPanel>
                
                <!-- Login Title -->
                <TextBlock Grid.Row="2" Text="Login" FontSize="24" FontWeight="Medium" 
                         Foreground="White" HorizontalAlignment="Center" 
                         Margin="0,0,0,30"/>
                
                <!-- License Key Input with Paste Button -->
                <Grid Grid.Row="3" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Background="#FF2A2438" CornerRadius="8" 
                          BorderBrush="#FF3D3650" BorderThickness="1">
                        <Grid>
                            <TextBox x:Name="LicenseKeyBox" 
                                   Background="Transparent" 
                                   Foreground="#FFA78BFA" 
                                   BorderThickness="0"
                                   FontSize="16" 
                                   FontFamily="Consolas"
                                   Padding="15,12" 
                                   VerticalContentAlignment="Center"
                                   CharacterCasing="Upper" 
                                   MaxLength="19"
                                   TextChanged="LicenseKeyBox_TextChanged"
                                   PreviewTextInput="LicenseKeyBox_PreviewTextInput"
                                   KeyDown="LicenseKeyBox_KeyDown"/>
                            <TextBlock x:Name="PlaceholderText" 
                                     Text="XXXX-XXXX-XXXX-XXXX" 
                                     Foreground="#FF6B7280" 
                                     FontSize="16" 
                                     FontFamily="Consolas"
                                     Margin="15,0,0,0" 
                                     VerticalAlignment="Center" 
                                     IsHitTestVisible="False"/>
                        </Grid>
                    </Border>
                    
                    <!-- Paste Button -->
                    <Button Grid.Column="1" x:Name="PasteButton" 
                          Style="{StaticResource IconButton}" 
                          Margin="8,0,0,0" 
                          ToolTip="Paste from clipboard"
                          Click="PasteButton_Click">
                        <TextBlock Text="📋" FontSize="18"/>
                    </Button>
                </Grid>
                
                <!-- Login Button -->
                <Button Grid.Row="4" x:Name="ActivateButton" 
                      Content="Log in" 
                      Style="{StaticResource PurpleLoginButton}" 
                      Margin="0,0,0,20"
                      Click="ActivateButton_Click" 
                      IsEnabled="False"/>
                
                <!-- Buy Now Button -->
                <Button Grid.Row="5" x:Name="BuyNowButton" 
                      Content="Buy Now" 
                      Style="{StaticResource SecondaryButton}" 
                      Click="BuyNowButton_Click"/>
                
                <!-- Status Message -->
                <TextBlock Grid.Row="6" x:Name="StatusMessage" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Top"
                         Margin="0,20,0,0"
                         FontSize="13" 
                         Foreground="#FF9CA3AF" 
                         Visibility="Collapsed">
                    <TextBlock.RenderTransform>
                        <TranslateTransform/>
                    </TextBlock.RenderTransform>
                </TextBlock>
            </Grid>
            
            <!-- Purple Glow Effects -->
            <Grid IsHitTestVisible="False">
                <!-- Top Left Glow -->
                <Ellipse Width="300" Height="300" 
                       HorizontalAlignment="Left" 
                       VerticalAlignment="Top" 
                       Margin="-150,-150,0,0"
                       Opacity="0.2">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#FF8B5CF6" Offset="0"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
                
                <!-- Bottom Right Glow -->
                <Ellipse Width="300" Height="300" 
                       HorizontalAlignment="Right" 
                       VerticalAlignment="Bottom" 
                       Margin="0,0,-150,-150"
                       Opacity="0.2">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#FFA78BFA" Offset="0"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
            </Grid>
        </Grid>
    </Border>
</Window>
