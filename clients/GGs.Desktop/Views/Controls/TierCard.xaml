<UserControl x:Class="GGs.Desktop.Views.Controls.TierCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="440">
    <UserControl.Resources>
        <Style x:Key="HoverableBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#192039" Offset="0"/>
                        <GradientStop Color="#1C1F35" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#2C3356"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            </Style>
    </UserControl.Resources>

    <Border x:Name="Root" Style="{StaticResource HoverableBorder}" Cursor="Hand" MouseLeftButtonUp="Root_MouseLeftButtonUp">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
<StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Fill="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               Foreground="#E8ECFF" FontWeight="SemiBold" Margin="8,0,0,0"/>
                </StackPanel>
                <TextBlock Text="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="#A6ADCC" FontSize="12" Margin="16,2,0,0"/>
            </StackPanel>

<StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                <TextBlock Text="{Binding Price, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="#C9D0F1" FontWeight="SemiBold"/>
                <Border x:Name="BuyBadge" Background="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=UserControl}}" CornerRadius="8" Padding="8,4" Opacity="0" Margin="12,0,0,0">
                    <TextBlock Text="Buy Now" Foreground="#1A1630" FontWeight="SemiBold"/>
                </Border>
            </StackPanel>
        </Grid>
        <Border.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="BuyBadge" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="BuyBadge" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
    </Border>
</UserControl>

