<Application x:Class="GGs.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:GGs.Desktop">
    <Application.Resources>
        <!-- Global scalable font size for accessibility -->
        <sys:Double x:Key="GlobalFontSize">13</sys:Double>
        
        <!-- Dark Theme Colors -->
        <Color x:Key="BackgroundColor">#FF0A0A0F</Color>
        <Color x:Key="SurfaceColor">#FF13131A</Color>
        <Color x:Key="SurfaceLightColor">#FF1A1A23</Color>
        <Color x:Key="BorderColor">#FF2A2A35</Color>
        <Color x:Key="AccentColor">#FFFF1464</Color>
        <Color x:Key="AccentLightColor">#FF00D9FF</Color>
        <Color x:Key="SuccessColor">#FF00FF88</Color>
        <Color x:Key="WarningColor">#FFFFAA00</Color>
        <Color x:Key="ErrorColor">#FFFF3366</Color>
        <Color x:Key="TextPrimaryColor">#FFFFFFFF</Color>
        <Color x:Key="TextSecondaryColor">#FF9999AA</Color>
        <Color x:Key="TextMutedColor">#FF666677</Color>
        
        <!-- Brushes from Colors -->
        <SolidColorBrush x:Key="Background" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="Surface" Color="{StaticResource SurfaceColor}"/>
        <SolidColorBrush x:Key="SurfaceLight" Color="{StaticResource SurfaceLightColor}"/>
        <SolidColorBrush x:Key="Border" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="Accent" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentLight" Color="{StaticResource AccentLightColor}"/>
        <SolidColorBrush x:Key="Success" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="Warning" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="Error" Color="{StaticResource ErrorColor}"/>
        <SolidColorBrush x:Key="TextPrimary" Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondary" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="TextMuted" Color="{StaticResource TextMutedColor}"/>
        
        <!-- Theme Resources (for dynamic theming) -->
        <SolidColorBrush x:Key="ThemeBackgroundPrimary" Color="#FF0A0A0F"/>
        <SolidColorBrush x:Key="ThemeBackgroundSecondary" Color="#FF13131A"/>
        <SolidColorBrush x:Key="ThemeBackgroundTertiary" Color="#FF1A1A23"/>
        <SolidColorBrush x:Key="ThemeAccentPrimary" Color="#FFFF1464"/>
        <SolidColorBrush x:Key="ThemeAccentSecondary" Color="#FF00D9FF"/>
        <SolidColorBrush x:Key="ThemeTextPrimary" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ThemeTextSecondary" Color="#FF9999AA"/>
        <SolidColorBrush x:Key="ThemeBorder" Color="#FF2A2A35"/>
        <SolidColorBrush x:Key="ThemeSuccess" Color="#FF00FF88"/>
        <SolidColorBrush x:Key="ThemeWarning" Color="#FFFFAA00"/>
        <SolidColorBrush x:Key="ThemeError" Color="#FFFF3366"/>
        
        <!-- Gradient Brushes -->
        <LinearGradientBrush x:Key="ThemeAccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FFFF1464" Offset="0"/>
            <GradientStop Color="#FF00D9FF" Offset="1"/>
        </LinearGradientBrush>
        
        <!-- Navigation Button Style -->
        <Style x:Key="NavButton" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle">
                <Setter.Value>
                    <Style TargetType="Control">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <Rectangle Stroke="{DynamicResource Accent}" StrokeThickness="2" SnapsToDevicePixels="True" Margin="2" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderThickness="0" Padding="24,0">
                            <Grid>
                                <Rectangle x:Name="indicator" Width="3" HorizontalAlignment="Left" 
                                         Fill="{DynamicResource Accent}" Visibility="Collapsed"/>
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="16,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="indicator" Property="Visibility" Value="Visible"/>
                                <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimary}"/>
                                <Setter TargetName="border" Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Stat Card Style -->
        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource SurfaceColor}" Offset="0"/>
                        <GradientStop Color="{StaticResource SurfaceLightColor}" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="Black" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Action Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
                        <GradientStop Color="{StaticResource AccentLightColor}" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="24,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle">
                <Setter.Value>
                    <Style TargetType="Control">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <Rectangle Stroke="{DynamicResource Accent}" StrokeThickness="2" SnapsToDevicePixels="True" Margin="2" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="8" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="scale"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scale" 
                                                       Storyboard.TargetProperty="ScaleX"
                                                       To="1.02" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="scale" 
                                                       Storyboard.TargetProperty="ScaleY"
                                                       To="1.02" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scale" 
                                                       Storyboard.TargetProperty="ScaleX"
                                                       To="1" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="scale" 
                                                       Storyboard.TargetProperty="ScaleY"
                                                       To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Base Control Style -->
        <Style TargetType="Control">
            <Setter Property="FontSize" Value="{DynamicResource GlobalFontSize}"/>
        </Style>
        
        <!-- Network Action Button Style -->
        <Style x:Key="NetworkActionButton" TargetType="Button">
            <Setter Property="Background" Value="#00d9ff"/>
            <Setter Property="Foreground" Value="#1a1d2e"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               CornerRadius="8"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Missing Theme Brushes for Enterprise Compatibility -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#10B981"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#EF4444"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#F59E0B"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#22C55E"/>
        
        <!-- Enterprise Theme Brushes -->
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#94A3B8"/>
        <SolidColorBrush x:Key="TextBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#64748B"/>
        
        <!-- Card Background Brushes -->
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#FFFFFF"/>
        
        <!-- Dark Theme Overrides -->
        <SolidColorBrush x:Key="DarkSurfaceBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="DarkBackgroundBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="DarkBorderBrush" Color="#334155"/>
        <SolidColorBrush x:Key="DarkTextPrimaryBrush" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="DarkTextSecondaryBrush" Color="#94A3B8"/>
        <SolidColorBrush x:Key="DarkTextMutedBrush" Color="#64748B"/>
    </Application.Resources>
</Application>
